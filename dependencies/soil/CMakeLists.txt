FILE(GLOB_RECURSE SOIL_SOURCES src/*.c)
FILE(GLOB_RECURSE SOIL_HEADERS src/*.h)

INCLUDE_DIRECTORIES(${CMAKE_CURRENT_SOURCE_DIR}/include)

ADD_LIBRARY(Soil ${SOIL_SOURCES} ${SOIL_HEADERS})
SET_TARGET_PROPERTIES(Soil PROPERTIES LINKER_LANGUAGE C COMPILE_FLAGS "")
